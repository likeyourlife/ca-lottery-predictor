# 2026-07-01 工作日志

## 天天乐每日预测流水线 (自动化首次执行)

- 修复 daily_pipeline.py 两处 bug：
  - `runner.run_backtest(records, window=100)` → `runner.run_backtest(window=100)` (BacktestRunner不接受records参数)
  - `records[-1]["date"]` → `records[-1]["draw_date"]` (字段名不匹配)
- 流水线成功完成5步：数据初始化(682期) → 100期回测 → 当日预测 → JSON输出 → 回测保存
- 回测结果：Top2 ✅(+1.71%), Top4 ✅(+5.38%), Top10 ❌(-4.37%)
- 预测输出：avoid Top10 = {4,26,24,12,22,13,34,7,15,39}, rebound Top10 = {4,7,26,10,39,17,28,24,13,31}
- 引擎权重：E3(马尔可夫)=36% > E4(联合)=34% > E2(贝叶斯)=22% > E1(频次)=8% > E5(FFT)=1% > E6(MC)=0%
